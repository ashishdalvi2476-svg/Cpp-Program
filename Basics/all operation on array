#include <iostream>
using namespace std;
int main()
{
   int n;
   cout<<"* Enter The Size Of An Array : "<<endl;
   cin>>n;
   int arr[n],fre[n];
   fre[n]=-1;
   cout<<"* Enter The Elements Of Array : ";
   for(int i=0;i<n;i++){
       cin>>arr[i];
   }
   cout<<"* Accessing the elements of array : "<<endl;
   for(int i=0;i<n;i++){
       cout<<arr[i]<<" ";
   }
   cout<<endl;
   int max=arr[0],min=arr[0];
   for(int i=0;i<n;i++){
      if(arr[i]>max){
          max=arr[i];
      }
 }
   cout<<"* Maxium Element is : "<<max<<endl;
   for(int i=0;i<n;i++){
      if(arr[i]<min){
          min=arr[i];
      }
   }
   cout<<"* Minimum  Element is : "<<min<<endl;
   
   cout<<"* Frequency Of Element is : "<<endl;
   for(int i=0;i<n;i++){
       int count=1;
       for(int j=i+1;j<n;j++){
           if(arr[i]==arr[j]){
               count++;
               fre[j]=0;
           } 
           
       }
      if(fre[i]!=0){
           fre[i]=count;
       }
   }
   for(int i=0;i<n;i++){
      if(fre[i]!=0){
          cout<<arr[i]<<" "<<fre[i]<<endl;
         
      }
   }
   cout<<"* Calculating the average value : "<<endl;
   int sum=0,avg =0;
   for(int i=0;i<n;i++){
       sum+=arr[i];
   }
   avg=sum/n;
   cout<<"* Average is : "<<avg<<endl;
   
} 
  

 
   for(int i=0;i<n;i++){
       sum+=arr[i];
   }
   avg=sum/n;
   cout<<"* Average is : "<<avg<<endl;
   
}
