class Solution {
public:
    bool isPalindrome(int x) { 
        int r;
        long long sum=0,n=x;
    
        while( x > 0){
        r=x%10;
        sum=sum*10+r;
        x=x/10;
    
        }
        if(sum==n){
        return true;
        }
        return false;
        
    }
    
};